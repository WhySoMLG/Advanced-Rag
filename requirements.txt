# Web Framework — FastAPI backend
# The React/Next frontend lives in ./frontend and talks to this server over JSON.
fastapi
uvicorn[standard]
python-multipart

# Local LLM Integration
# Drives chunking, contextual retrieval, query rewriting (multi-turn),
# and answer generation — all locally via Ollama.
ollama

# Vector Database & Retrieval
# ChromaDB stores per-chunk metadata (source, modality, indexed_at,
# indexed_ts) and is queried with $and / $in / $gte / $lte for the
# UI metadata filters.
chromadb
rank_bm25

# Cross-Encoder Reranker (optional — enable with RAGConnector(use_reranker=True))
# Versions are pinned to a known-good combination:
#   • transformers 5.x reorganised internal modules and breaks sentence-transformers
#   • sentence-transformers 5.x + recent accelerate trigger a "meta tensor" load
#     error on CPU. The 3.x line is the last stable family for CrossEncoder on CPU.
transformers>=4.40,<5
sentence-transformers>=3.0,<4

# Web URL Ingestion
# httpx fetches pages; trafilatura strips boilerplate and returns clean Markdown.
httpx
trafilatura

# Document Parsing (PDFs, Word, etc.)
docling

# Video & Audio Processing
ffmpeg-python
openai-whisper

# Miscellaneous
pydantic
